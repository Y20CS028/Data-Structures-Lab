void reversePrint(SinglyLinkedListNode* llist) {
int a[10000],i=0,c=0;
while(llist!=NULL)
{
    a[i]=llist->data;
    i=i+1;
    c=c+1;
    llist=llist->next;
}
for(i=c-1;i>=0;i--)
{
    printf("%d\n",a[i]);
}
}

